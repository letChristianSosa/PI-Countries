const {Router} = require('express');

const router = Router();


router.get('/', (req,res) => {

});

router.get('/{idPais}', (req,res) => {

});

router.get('/?name', (req,res) => {

});

module.exports = router;   